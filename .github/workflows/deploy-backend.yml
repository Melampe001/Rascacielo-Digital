name: 🏛️ Deploy Backend (Node.js → Vercel)

on:
  push:
    branches: [ Main, main ]
    paths:
      - 'api/**'
      - 'agents/**'
      - 'modules/**'
      - 'package.json'

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run Tests
        run: npm test

      - name: 🔍 Lint
        run: npm run lint

      - name: 🏗️ Build
        run: npm run build

      - name: 🔒 Security Audit
        run: npm audit --audit-level=moderate

      - name: ☁️ Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
